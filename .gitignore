# 가상환경 (각자 자기 PC에서 만들어야 합니다)
.venv/
.venv-tf/

# 내려받은 MNIST 데이터셋 (train.py 가 자동으로 받습니다)
data/

# 파이썬 캐시
__pycache__/
*.pyc

# 바로 가기 파일
# 만든 PC의 절대 경로와 컴퓨터 이름이 내부에 기록되므로 저장소에 올리지 않습니다.
# 내려받은 뒤 바로가기_만들기.ps1 을 실행하면 각자의 환경에 맞게 생성됩니다.
*.lnk
